#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "algorithms.h"

int main() {
  printf("length of last word: %d\n", length_of_last_word("a    bx   d "));

  return EXIT_SUCCESS;
}
